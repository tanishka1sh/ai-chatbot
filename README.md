# ai-chatbot
An AI chatbot using Python, LLM, and FastAPI is a backend application that takes user messages through an API, sends them to a large language model (LLM), and returns smart, human-like replies in real time.
